# GenAI ChatBot

A Flask-based chatbot.

## Features
- File upload support (PDF, DOCX, TXT)
- Docker containerization

## Quick Start

### Option 1 Docker (Recommended)
```bash
# Start all services
docker-compose up -d

# Access frontend at http://localhost:3000
# Backend API at http://localhost:5000